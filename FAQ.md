# FAQ (คำถามที่พบบ่อยและวิธีแก้ไข)

รวบรวมปัญหาที่เคยเจอและวิธีแก้ไขจากประสบการณ์จริง เพื่อเตือนความจำและเป็นคู่มือในการปรับแต่งระบบค่ะ

## 1. ปัญหาบอท OpenClaw ไม่ยอมตอบในกลุ่ม Telegram (โดนบล็อก / Not-allowed)
**ปัญหา:** เชิญบอทเข้ากลุ่มแล้ว แต่บอทไม่ตอบสนอง หรือในระบบฟ้องว่าไม่ได้รับอนุญาต (Dropped as not-allowed)
**สาเหตุ:** เป็นบั๊กของ OpenClaw เวอร์ชันใหม่ (อ้างอิง Issue #40444) ที่ระบบ Validation เข้มงวดเกินไป โดยใช้ Regex (`^\d+$`) ตรวจสอบ ID ให้รับเฉพาะตัวเลขบวก แต่ ID กลุ่มของ Telegram มักจะขึ้นต้นด้วยเครื่องหมายลบ (เช่น `-5200416075`) ระบบจึงปฏิเสธ ID นั้นทันที
**วิธีแก้ไข:**
- ปรับแก้ค่าคอนฟิกของ OpenClaw โดยตั้งค่า `groupPolicy: "allowall"` เพื่ออนุญาตให้บอทตอบได้ทุกกลุ่ม (ต้องระวังหากบอทถูกเชิญไปกลุ่มที่ไม่รู้จัก)
- ใช้งานผ่านแชทส่วนตัว (DM) แทนชั่วคราว เนื่องจาก User ID ของบุคคลจะเป็นตัวเลขบวกและใช้งานได้ปกติ

## 2. ปัญหาบอทในกลุ่ม Telegram ไม่เห็นข้อความถ้าไม่แท็ก (@)
**ปัญหา:** ต้องพิมพ์ `@ชื่อบอท` ตลอด บอทถึงจะยอมตอบ
**สาเหตุ:** เป็นระบบความปลอดภัยพื้นฐาน (Group Privacy) ของทาง Telegram เอง
**วิธีแก้ไข:** ให้ผู้สร้างบอท (แอดมิน) เข้าไปคุยกับ `@BotFather` ใน Telegram แล้วใช้คำสั่ง `/setprivacy` จากนั้นเลือกบอทของเราและตั้งค่าเป็น **Disable** เพื่อให้บอทมองเห็นทุกข้อความในกลุ่ม

## 3. ปัญหาอัปโหลดไฟล์ขึ้น GitHub ไม่ผ่าน (Error 403 หรือ Authentication Failed)
**ปัญหา:** รันคำสั่ง `git push` แล้วติด Error 403 หรือฟ้องว่า Password authentication is not supported
**สาเหตุ:** GitHub ยกเลิกการใช้รหัสผ่านบัญชีปกติในการ Push โค้ดผ่าน Command Line แล้ว และในกรณีที่ใช้ Fine-grained Token อาจจะยังไม่ได้เปิดสิทธิ์ Write
**วิธีแก้ไข:** 
- ไปสร้าง **Personal Access Token (PAT)** แบบ **Classic** บนเว็บ GitHub แล้วติ๊กให้สิทธิ์ในส่วนของ `repo` ทั้งหมด และนำ Token ที่ขึ้นต้นด้วย `ghp_` มาใช้แทนรหัสผ่าน
- หากใช้ Token แบบ Fine-grained ต้องไปปรับตั้งค่า Repository permissions ในหมวด Contents ให้เป็น **Read and write**